apiVersion: v1
kind: Service
metadata:
  name: fee-service
  labels:
    app: fee-service
spec:
  type: ClusterIP
  selector:
    app: fee-service
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
      name: http