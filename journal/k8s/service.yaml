apiVersion: v1
kind: Service
metadata:
  name: journal-service
  labels:
    app: journal-service
spec:
  type: ClusterIP
  selector:
    app: journal-service
  ports:
    - port: 8087
      targetPort: 8087
      protocol: TCP
      name: http